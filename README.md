# blog-assets

구글 블로거(asomegs.blogspot.com) 글에 넣는 사진을 GitHub Pages로 서빙하는 저장소.

- 서빙 주소: `https://asomegs.github.io/blog-assets/<slug>/<파일>`
- 폴더 하나가 글 하나(`<slug>/`). 파일은 `contents-publisher` 파이프라인의 `blogger.py assets`가 넣는다.
- 손으로 파일을 옮기거나 지우지 않는다. 발행된 글이 이 주소를 그대로 참조한다.
